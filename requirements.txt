numpy>=1.20.0
h5py>=3.0.0
pyyaml>=5.4.0
rich>=10.0.0
matplotlib>=3.0.0

# Unitree SDK (local package)
# Install separately: cd ../unitree_sdk2_python && uv pip install -e .
# Or install from path:
-e ../unitree_sdk2_python
